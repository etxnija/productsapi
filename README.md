# productsapi

* I didn't add a DB, mysql, since I ran into different kind of problems with access. Tried both local and docker. 
* Only added acceptence test(API tests) since it tested what was needed. There was not much logic to be tested on a lower level in what I implmented.
 